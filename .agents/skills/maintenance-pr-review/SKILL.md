---
name: maintenance-pr-review
description: Review automated maintenance PRs for hidden regressions, low-signal bulk edits, and unsafe cleanup changes. Use when evaluating chore/maintenance PRs generated by automation or bots.
---

# Maintenance PR Review

## Workflow
1. Separate mechanical updates from behavioral changes.
2. Prioritize runtime-risk areas: state management, exports, persistence, and workflows.
3. Validate that cleanup does not remove required wiring or assets.
4. Demand tests for any non-trivial logic changes.
5. Summarize findings with required fixes before merge.

## Red Flags
- Large mixed PRs with both formatting and logic edits.
- Removed files without clear replacement path.
- Changed defaults/config values without migration notes.

## Project Lessons Encoded
- Repeated maintenance PR flow required targeted review (`PR #5`, `PR #10`, `PR #12`, `PR #19`, `PR #39`, `thread: Review PR for maintenance changes`).

## Anti-Misdirection Guardrails
- Verify assumptions against the current codebase and runtime behavior before proposing or applying fixes.
- Prefer integrated validation (real backend/runtime flows) for behavior-critical or release-critical claims; do not rely on mock-only confidence.
- Do not claim completion without concrete evidence (commands run, tests executed, or reproducible manual checks).
- Preserve user data and local changes; avoid destructive git/file operations unless explicitly requested.
- If repository policy blocks direct action (for example protected `main`), follow the required PR flow instead of bypassing safeguards.
- State remaining uncertainty or residual risk explicitly instead of implying absolute certainty.
