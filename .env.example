# Google OAuth client ID (Desktop App client from Google Cloud Console)
NEXT_PUBLIC_GOOGLE_CLIENT_ID=
# Optional: required only for web callback flow when OAuth client enforces a secret.
# Do not set this unless you explicitly need browser-based OAuth callback mode.
NEXT_PUBLIC_GOOGLE_CLIENT_SECRET=

# Optional: enable bundle analysis for Next build
# ANALYZE=true
