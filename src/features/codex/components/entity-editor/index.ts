/**
 * Entity Editor Components
 * 
 * Decomposed from entity-editor.tsx for better maintainability.
 * Each component handles a specific concern:
 * 
 * - EntityEditorHeader: Navigation and action buttons (back, save, delete)
 * - EntityEditorInfoCard: Entity info display (thumbnail, name, category, tags)
 */

export { EntityEditorHeader } from './EntityEditorHeader';
export { EntityEditorInfoCard } from './EntityEditorInfoCard';
