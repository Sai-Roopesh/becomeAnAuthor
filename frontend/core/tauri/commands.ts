/**
 * Tauri command surface.
 *
 * Implementation is split by domain under ./command-modules to keep files small
 * and reduce blast radius when changing command wrappers.
 */

export * from "./command-modules";
